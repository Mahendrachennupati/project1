import { TrimeSpaceDirective } from './trime-space.directive';

describe('TrimeSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimeSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
